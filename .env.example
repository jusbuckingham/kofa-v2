# ──────────────────────────────────────────────────────────────────────────────
# Kofa.ai — Example Environment Variables
# Copy this file to .env.local and fill in with real values.
# ──────────────────────────────────────────────────────────────────────────────

# 1) Database (MongoDB Atlas)
MONGODB_URI=your-mongodb-uri
MONGODB_DB_NAME=kofa

# 2) OpenAI (Summarization)
OPENAI_API_KEY=your-openai-api-key
MAX_TO_SUMMARIZE=40

# 3) Public Site (URLs)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_DEFAULT_LENS=black

# 4) NextAuth (Authentication)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret

# 5) SMTP (Email Provider)
EMAIL_SERVER_HOST=smtp.example.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your-user
EMAIL_SERVER_PASSWORD=your-password
EMAIL_FROM="Kofa AI <no-reply@kofa.ai>"

# 6) Stripe (Billing)
STRIPE_SECRET_KEY=your-stripe-secret
STRIPE_PRICE_ID=your-stripe-price-id
STRIPE_WEBHOOK_SECRET=your-stripe-webhook-secret

# 7) Metered Paywall
ALLOW_DEV_ANON_READS=true
FREE_READS_PER_DAY=3

# 8) RSS Feeds (comma-separated)
FEED_URLS=https://rss.nytimes.com/services/xml/rss/nyt/HomePage.xml,https://feeds.bbci.co.uk/news/rss.xml

# 9) Cron (Vercel)
CRON_SECRET=your-cron-secret

# 10) External News APIs (optional)
GNEWS_API_KEY=your-gnews-key
NEWSDATA_API_KEY=your-newsdata-key

# 11) Ingestion & Ranking Lens
NEWS_LENS=black
NEWS_MIN_SCORE=4
NEWS_ALLOWLIST_ONLY=0
NEWS_BLACK_DOMAIN_BOOST=2

# ──────────────────────────────────────────────────────────────────────────────
# END
# ──────────────────────────────────────────────────────────────────────────────